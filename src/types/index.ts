export interface Section {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface DiagramExample {
  id: string;
  title: string;
  description: string;
  imageUrl: string;
}